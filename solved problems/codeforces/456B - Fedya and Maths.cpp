#include bitsstdc++.h
using namespace std;

int main() {
	string n;
	cinn;
	bool chk = false;
	if(n.size() = 2){
		stringstream ss(n);
		int a;
		ssa;
		if(a%4 == 0) chk = true;
	}else{
		int a,b,num;
		a = n[n.size()-1]-'0',b = n[n.size()-2]-'0';
		num = b10 + a;
		if(num%4 == 0) chk = true;
	}
	(chk) printf(4n)printf(0n);
	return 0;
}